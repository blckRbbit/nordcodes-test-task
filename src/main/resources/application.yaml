server:
  port: 9009

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/nordcodes_DB
    username: root
    password: 11

utils:
  url:
    prefix: https://
    postfix: .rbbt